name: website
description: My personal website

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_shaders: ^0.1.2
  flutter_animate: ^4.2.0+1
  provider: ^6.0.5
  url_launcher: ^6.1.14
  html: ^0.15.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - 'assets/images/github-text-logo.png'
    - 'assets/resume.pdf'

  shaders:
    - 'assets/shaders/gradient.frag'
