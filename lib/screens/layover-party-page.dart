import 'package:flutter/material.dart';

class LayoverPartyPage extends StatelessWidget {
  const LayoverPartyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Layover Party Page'));
  }
}
